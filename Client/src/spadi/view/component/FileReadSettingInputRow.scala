package spadi.view.component

import java.nio.file.Path

import utopia.reflection.component.context.TextContext
import utopia.reflection.container.stack.segmented.SegmentedGroup

/**
 * Used for inputting file read settings
 * @author Mikko Hilpinen
 * @since 26.5.2020, v1.1
 */
class FileReadSettingInputRow(group: SegmentedGroup, path: Path)(implicit context: TextContext)
{
	// ATTRIBUTES   ------------------------------
	
	val asd = context.forGrayFields.use { implicit ddC =>
		// val shopSelection =
		// TODO: Continue by creating the input fields
	}
}
