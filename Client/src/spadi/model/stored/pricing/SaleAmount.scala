package spadi.model.stored.pricing

/**
  * Represents a stored sale percentage
  * @author Mikko Hilpinen
  * @since 31.7.2020, v1.2
  */
case class SaleAmount(id: Int, groupId: Int, priceModifier: Double)
